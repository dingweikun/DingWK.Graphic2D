<Window x:Class="WpfTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTest"
        xmlns:g2d="clr-namespace:DingWK.Graphic2D.Wpf.GraphicVisuals;assembly=DingWK.Graphic2D.Wpf"
        xmlns:ctr="clr-namespace:DingWK.Graphic2D.Wpf.Controls;assembly=DingWK.Graphic2D.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <ctr:GraphicVisualCanvas x:Name="cc" Background="LightBlue">
            <!--<g2d:GraphicVisualHost x:Name="host"/>-->
        </ctr:GraphicVisualCanvas>

        <StackPanel Background="Gray" Grid.Column="1">
            <TextBlock>Page Size</TextBlock>
            <TextBox Text="{Binding ElementName=cc,Path=PageSize, Mode=TwoWay}"/>
            <TextBox x:Name="txt" Background="LightGreen"/>
            <Button Click="Button_Click">btn</Button>
            <TextBox Text="{Binding ElementName=cc,Path=PageOffsetX, Mode=TwoWay}"/>
            <TextBox Text="{Binding ElementName=cc,Path=PageOffsetY, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</Window>
